// fib(4) // 3
// fib(10) // 55
// fib(28) // 317811
// fib(35) // 9227465


let array = [1, 1];

function fib(i) {

    if(i + 1 === num) {
        return array;
    }

    return array + fib(array[i + 1]);
}

fib(4)